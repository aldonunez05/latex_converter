**Deep Learning Model to convert handwritten notes to latex**

Cody - GPU acceleration in CUDA 

Alexia - Frontend/UI

The command to host a local server is:
```bash
python3 -m http.server [port]
```

if you want to add new things make a new branch and then submit a pull request to merge

we also need more data of handwritten text and math symbols 
